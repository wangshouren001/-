计算机工作原理：
输入设备输入待处理的数据和处理方式（指令）到内存（包含各级缓存），cpu根据指令做计算（四则、逻辑），计算完毕把得到的结果给输出设备。
这里面包含三个步骤：
    		1、数据和指令的输入
		2、第一步内容的临时存储
		3、计算，以及计算结果的临时存储
		
备注：当然真实的过程相当复杂,但是从功能上讲，如何让计算机跑起来，按照我们的意愿跑起来，
只需要关注：数据、控制、计算 这三个方面。而其他的一切都是围绕这三个方面展开的。
